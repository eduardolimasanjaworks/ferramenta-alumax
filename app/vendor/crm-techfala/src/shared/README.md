# shared/

Código compartilhado sem libs externas.

- `icons.tsx` — SVGs Lucide do dump
- `navIcons.tsx` — ícones da sidebar Bubble
- `lib/storage.ts` — localStorage
- `types/crm.ts` — tipos do Kanban
