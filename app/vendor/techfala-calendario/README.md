# TechFala Calendário

Aplicação CRM TechFala com o módulo de Calendário (Vite + React + TypeScript).

## Rodar local

```bash
npm install
npm run dev
```

Referências Bubble (dumps de UI) ficam em `referencia-bubble/`.
